apply plugin: 'java'

group = 'enn-monitor'
version = "${versions.ennew_monitor_framework_ai}"
jar {
	baseName = 'enn-monitor-framework-ai-nn'
}

dependencies { 
	compile libraries.enn_monitor_framework_log_core
}